name: arabic_verb_final
description: A modern Arabic verb conjugation app
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  provider: ^6.0.5
  cupertino_icons: ^1.0.6  # ← Add this just in case

flutter:
  uses-material-design: true  # ← Only this line under `flutter:`